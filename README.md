# Raveum

A modern React Native application built with Expo, featuring a beautiful and responsive homepage with reusable components.

## Features

- 🏠 **Modern Homepage Layout** - Clean and intuitive design
- 🎨 **Reusable Components** - Modular and maintainable code structure
- 📱 **Responsive Design** - Optimized for mobile devices
- 🎭 **Accordion Info Cards** - Expandable/collapsible information sections
- 🏘️ **Property Carousel** - Interactive overlapping card layout with animations
- ⭐ **Testimonial Section** - Carousel with pagination dots
- 🎯 **Theme System** - Consistent colors, typography, and spacing

## Project Structure

```
src/
├── components/         # Reusable UI components
│   ├── Header
│   ├── InfoCard
│   ├── PropertyCard
│   ├── TestimonialCard
│   ├── MenuCard
│   └── SectionHeader
├── screens/           # Screen components
│   └── HomeScreen
├── navigation/        # Navigation configuration
├── theme/            # Styling configuration (colors, typography, spacing)
├── constants/        # App-wide constants and data
├── hooks/            # Custom React hooks
├── utils/            # Utility functions
├── context/          # React Context providers
└── config/           # App configuration files
```

## Technologies Used

- **React Native** - Mobile framework
- **Expo** - Development platform
- **React Navigation** - Navigation library
- **Expo Vector Icons** - Icon library

## Installation

1. Clone the repository:
```bash
git clone https://github.com/anushaAnu333/Raveum.git
cd Raveum
```

2. Install dependencies:
```bash
npm install
```

3. Start the development server:
```bash
npm start
```

4. Run on your device:
- Scan the QR code with Expo Go app (Android)
- Scan the QR code with Camera app (iOS)

## Available Scripts

- `npm start` - Start the Expo development server
- `npm run android` - Run on Android device/emulator
- `npm run ios` - Run on iOS device/simulator
- `npm run web` - Run in web browser


